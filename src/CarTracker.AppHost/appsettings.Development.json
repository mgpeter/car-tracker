{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Parameters": {
    "//": "Local development default. A parameter with no value leaves the AppHost blocked on a dashboard modal with nothing in its log to say so — user-secrets alone are not enough, because they only load when the environment is Development. Override in production via the Parameters__postgres-password environment variable.",
    "postgres-password": "devpassword"
  }
}
