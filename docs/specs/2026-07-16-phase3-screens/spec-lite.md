# Spec Summary (Lite)

Written after the fact: the seven screens it describes shipped in one session before this spec existed, so it is
Complete on creation — the spec trail catching up with tested code.

Give the remaining workbook sheets a home. Seven screens — tasks, issues, tyres, wash, budget, equipment,
vehicle-info — each deriving what the design hardcodes (the task bundle from open Workshop estimates, wash
cadence from the gaps, budget variance from the expense rows, issue duration from the first-noted date). Only
documents is left for a later spec, because it needs file upload, which nothing else does.
