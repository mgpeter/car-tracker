# Cambelt deployment configuration.
#
# THERE ARE TWO DEPLOYMENT MODES AND THIS FILE COVERS BOTH. The values below are the STANDALONE defaults (the
# Synology, or any single box); the shared-host column notes what changes on Asgard. See the README's
# deployment section for the full picture.
#
#   NAS          copy this to `.env` beside `docker-compose.nas.yml` and fill it in. That file is the whole
#                machine in one document - import it into Container Manager as a Project, or run
#                  docker compose up -d
#                beside it over SSH. No `-f`, no `--profile`: DSM's Project UI can pass neither.
#
#   Any other    the same standalone stack driven from the two source files instead, which is what
#   single box     docker compose -f docker-compose.yml -f docker-compose.standalone.yml --profile standalone up -d
#                does. Identical result; the NAS file exists because Container Manager cannot say that.
#
#   Shared host  DO NOT COPY THIS FILE THERE. The tenant .env is rendered from Azure Key Vault by the host
#                into /srv/infra/tenants/cambelt/.env and passed with --env-file. It must never be committed,
#                and it must never be placed under /srv/data/cambelt/ - that tree is what the host's restic
#                job copies off-box, so a secrets file inside it is a secrets file in the backups.
#
# The real .env is git-ignored either way - never commit it (it holds the DB password).

# Docker Hub namespace the images are published under (see the CI publish and release workflows - since
# DEC-021 they are the only things that push, and scripts/release.* cannot).
DOCKERHUB_USER=mgpeter

# Image tag to run. There are three channels, and they answer different questions.
#
#   stable   the release channel. Moves only when a `v<version>` git tag is pushed, which is a deliberate
#            act taken after a version has proven itself. `latest` is the same digest under the name a bare
#            `docker pull` resolves; prefer `stable`, because the name says what the channel is.
#   edge     the tip of main. Moves on every commit that can affect an image. This is the dogfooding
#            channel: it is what finds the bugs, and it is what the NAS should run.
#   0.22.0   frozen. Nothing moves it, ever, so Watchtower cannot move you either.
#
#   NAS          `edge`. It is the box things get broken on, and taking every build within five minutes is
#                the point of it - docker-compose.nas.yml therefore defaults to `edge` rather than `latest`,
#                so this key can be left out there entirely. Set `stable` for a quieter box.
#   Shared host  PIN AN EXACT VERSION. There is one image per service and it works for any Auth0 tenant, so
#                the tag is the only thing that decides what runs; pinning also means the host's Watchtower
#                will not move you, because a semver tag is immutable. Production then changes when you change
#                this line and at no other time, which is the intent for a public site.
#
# WATCHTOWER FOLLOWS THE TAG THE CONTAINER WAS CREATED FROM, not the one in this file. Changing this line
# needs `docker compose up -d` - Container Manager's *Build* - because a Restart reuses the existing spec.
# `docker inspect --format '{{.Config.Image}}' <container>` is what actually answers which channel you are on.
TAG=stable

# Postgres password.
#
#   Standalone   the superuser password, read only on first init of the data directory.
#   Shared host  the password of the tenant's own `cambelt` role. The role is not a superuser and cannot
#                create databases or install extensions; the host makes the database and the role.
#
# Deliberately no `:?` guard on this in the compose file, so `docker compose config` works without an .env for
# validation. A blank one is not silent: Postgres refuses the connection and the container fails at boot.
POSTGRES_PASSWORD=change-me

# The database and role to connect as.
#
#   NAS          leave unset. docker-compose.nas.yml defaults to cartrackerdb / postgres, which is what an
#                existing ${DATA_ROOT}/pgdata was initialised with. PGUSER MUST BE THE SUPERUSER THAT DATA
#                DIRECTORY WAS CREATED WITH: the postgres service creates exactly one role, and naming any
#                other gives a Postgres whose healthcheck passes - pg_isready does not authenticate - and a
#                webapi that cannot log in.
#   Shared host  PGDATABASE=cambelt and PGUSER=cambelt, both created by the host, which is also what
#                docker-compose.yml defaults to.
#
# The two files therefore default differently, deliberately. This line used to claim the tenant file defaulted
# to cartrackerdb/postgres "exactly as before"; it does not, and never did after DEC-020 split them.
#
# `Maximum Pool Size=20` is appended in the compose file and is not optional on a shared server: Npgsql
# defaults to 100 connections PER CONNECTION STRING against a cluster configured for 150 IN TOTAL, so two
# tenants at the default can starve the third.
#PGDATABASE=cambelt
#PGUSER=cambelt

# Where Postgres data, uploaded documents and backups live (bind mounts - they survive down -v, rebuild and
# destroy; only deleting the host folder removes them).
#
#   Standalone   create ${DATA_ROOT}/pgdata, ${DATA_ROOT}/documents and ${DATA_ROOT}/backups before the first
#                `up`. The documents folder holds the uploaded files themselves and is NOT covered by the
#                db-backup sidecar (which dumps Postgres only), so back it up alongside ${DATA_ROOT}/backups.
#   Shared host  /srv/data/cambelt, and EVERY bind mount must stay under it: that is precisely the tree the
#                host snapshots, and bytes written anywhere else are not backed up with nothing to say so.
DATA_ROOT=/volume1/docker/cartracker

# Public host port for the gateway. STANDALONE ONLY - it is supplied by docker-compose.standalone.yml, and the
# base file publishes no ports at all. On the shared host Caddy is the only listener and a published port is a
# drift-check failure and a real internet exposure, because Docker's DNAT sits in PREROUTING while ufw filters
# INPUT. Must match the origin registered in Auth0 (http://synologynas:8082).
GATEWAY_PORT=8082

# .NET garbage collector. 1 = server GC (a heap per core, faster, hungry), 0 = workstation.
#
#   Standalone   leave unset; it defaults to 1, which is what the NAS has always run.
#   Shared host  0. Server GC reserves per-core heaps and is a real memory hog beside three or four
#                neighbours on an 8 GiB VM.
#
# It is named explicitly in the compose file because --env-file only feeds ${...} interpolation; it does not
# inject anything into a container. Without that line this value would go nowhere.
#DOTNET_gcServer=0

# The Auth0 API (resource server) tokens are validated against, and the tenant that issues them.
#
#   Standalone   leave unset - defaults to cartracker.api on the usualexpat tenant.
#   Shared host  AUTH0_AUDIENCE=cambelt.api
#
# THE AUDIENCE IS A SECURITY BOUNDARY, NOT A LABEL. A bearer token is checked on signature, issuer, audience
# and expiry, and nothing checks which client asked for it - so while two deployments share an audience, a
# token minted against a box on a home LAN is cryptographically valid on the public site.
#
# AUTH0_AUDIENCE is read by BOTH halves: the webapi validates tokens against it, and the gateway hands it to
# the browser so the SPA requests tokens for it. They are two ends of one check and a mismatch is a 401 on
# every request after an apparently successful login.
#AUTH0_AUDIENCE=cambelt.api
#AUTH0_AUTHORITY=https://usualexpat.uk.auth0.com/

# --- The browser's Auth0 application ------------------------------------------------------------------------
# The gateway serves these to the SPA as /config.js and names AUTH0_SPA_DOMAIN in the Content-Security-Policy
# it emits, so the tenant the browser is permitted to call and the tenant it does call come from one place.
#
# THIS IS WHAT MAKES THE APP SELF-HOSTABLE. Until 0.21.0 these were compiled into the JS bundle by Vite, so
# running this against your own Auth0 tenant meant forking the repo and building your own gateway image, and
# the two deployments here needed two images per release.
#
# Blank falls back to this project's own application - the Lookup__* polarity, not the Signup__* one - so an
# existing .env keeps working with nothing added. Set all three to point a deployment at your own tenant:
#AUTH0_SPA_DOMAIN=your-tenant.eu.auth0.com
#AUTH0_SPA_CLIENT_ID=your-spa-application-client-id

# Timezone for backup timestamps and Watchtower schedules. Standalone only in practice - both services that
# read it are behind the standalone profile.
TZ=Europe/London

# --- DVLA/DVSA registration lookup (optional) -------------------------------------------------------------
# Leave every value blank and the add-car lookup answers 503 "not configured" while manual entry works exactly
# as before. Where the credentials come from is in the README Quickstart; the short version:
#   VES  - https://register-for-ves.driver-vehicle-licensing.api.gov.uk/ (manual approval, key by email)
#   MOT  - a separate DVSA registration giving a key plus OAuth client credentials
# VES ALONE IS ENOUGH: it gates the feature and returns make/colour/year/engine/fuel/tax. The four MOT values
# are all-or-nothing and add only the MOT expiry seed, so a lagging DVSA approval blocks nothing.
VES_API_KEY=
MOT_API_KEY=
MOT_TOKEN_URL=
MOT_CLIENT_ID=
MOT_CLIENT_SECRET=

# --- Who may sign up --------------------------------------------------------------------------------------
# SIGN-UP IS OPEN BY DEFAULT, AND THIS POLARITY FLIPPED IN 0.24.0. A blank `Signup__*` used to mean the DOOR
# IS SHUT; it now means ANYONE the Auth0 tenant authenticates gets an account. If you are reading this file
# because you remember the old rule, that is the line to reread.
#
# The reason is that the door stopped being what protects a deployment. An account on its own costs nothing -
# it is rows in a database nobody else can see. The three things that DO cost something are each bounded by a
# plan allowance a stranger does not have: the assistant's model tokens, the documents volume and the DVLA
# quota. See the Plans block below, which is now the block that matters.
#
# ONLY `SIGNUP_MODE` DECIDES THIS. The allowlist below never does: with the mode open, a populated
# SIGNUP_ALLOWED_* is read for nothing, so a file that lists four addresses still admits the whole internet.
# The boot log says so when it happens; delete the lines or set the mode.
#
# Set `SIGNUP_MODE=InviteOnly` for a private instance and the old behaviour comes back whole: only a verified
# address on the lists below is provisioned, everyone else gets a 403 "not yet invited" and no user row is
# created, and empty lists then mean nobody. Existing accounts are never affected either way - these decide
# who may join, not who may stay.
#
# UNDER InviteOnly THE ADDRESS MUST ALSO BE VERIFIED BY THE TENANT (`email_verified`). On a database
# connection anyone can self-register with any address they can type, so a domain here would otherwise admit
# whoever registers as `anything@example.com` - the deployment reads as invitation-only and is open to the
# internet. A connection that never verifies addresses therefore admits nobody, whatever is listed.
#
# COMMA-SEPARATED, and deliberately not indexed lists (`Signup__AllowedDomains__0=`): an indexed key left empty
# injects an entry of "", and an empty entry matches every address alive.
SIGNUP_MODE=
SIGNUP_ALLOWED_EMAILS=
SIGNUP_ALLOWED_DOMAINS=

# --- Plans: what an account may spend ----------------------------------------------------------------------
# >>> IF YOU SET NOTHING ELSE IN THIS FILE, SET PLANS_COMP_EMAILS. <<<
# THE COMP LIST IS THE THING TO SET, and on a fresh deployment it is the one people forget - it was forgotten
# on cambelt.app's own first 0.24.0 deploy, which is why it now shouts. Blank comps
# NOBODY, which means every account - INCLUDING YOURS - is on the free tier and the assistant is switched off
# for all of them. The API logs a warning naming this at every boot, and the account screen says
# "no account on this deployment is on the paid tier - Plans:CompEmails is empty" rather than leaving you to
# work it out from a missing button.
#
# Same rules as the sign-up lists: comma-separated, `@example.com` and `example.com` both work, and the
# address must be VERIFIED by the tenant. Today this is the only way onto the paid tier; a subscription will
# be the second, and this list stays as the staff-and-friends door.
PLANS_COMP_EMAILS=
PLANS_COMP_DOMAINS=

# The allowances themselves. BLANK MEANS THE SHIPPED DEFAULT for that one number and nothing else changes, so
# set only what you want to move:
#
#                        Free     Pro
#   Assistant            off      on
#   Documents            100      2,000     (25 MB a file on both - that is not a plan setting)
#   DVLA lookups/day     3        50
#
# Pro deliberately sets no chat-token ceiling of its own and defers to CHAT_DAILY_TOKENS_PER_OWNER below, so
# a deployment's model spend stays configured in one place.
PLANS_FREE_MAX_DOCUMENTS=
PLANS_FREE_DAILY_VEHICLE_LOOKUPS=
PLANS_PRO_MAX_DOCUMENTS=
PLANS_PRO_DAILY_VEHICLE_LOOKUPS=

# --- Legal: who is responsible for the data on this deployment ----------------------------------------------
# BLANK MEANS NO POLICY IS PUBLISHED, WHICH IS THE OPPOSITE POLARITY TO `SIGNUP_MODE` ABOVE. Read that
# sentence twice if you are copying this file forward: a blank Signup block opens the door, and a blank Legal
# block publishes nothing. Both defaults are the safe one for their own question, which is why they differ.
#
# With these two unset there is no /privacy, no /cookies and no /terms, no links to them in the footer, and no
# acceptance line beside the sign-up button. That is the RIGHT default for a NAS or a fresh checkout, because
# a committed policy naming somebody else as the controller would tell your household to write to a stranger
# about their own data. It is a real gap the moment SIGNUP_MODE is open, and the boot log warns when both are
# true at once.
#
# Set both to publish. The prose is committed in the app; what these supply is who it names.
LEGAL_CONTROLLER_NAME=
LEGAL_CONTROLLER_CONTACT=

# Optional. A postal address, and one sentence about where this runs and where its backups go - the app cannot
# work the second one out for itself, because the host left this repository (DEC-020). Blank omits each from
# the pages rather than printing an empty heading.
LEGAL_CONTROLLER_ADDRESS=
LEGAL_HOSTING_SUMMARY=

# Whose law the terms are read under. Blank means `United Kingdom`. Unlike the two names above, forgetting this
# does not stop the pages publishing.
LEGAL_JURISDICTION=

# --- Retention: how long the operational ledgers are kept ---------------------------------------------------
# BLANK MEANS 400 DAYS. `0` MEANS NEVER PRUNE. That is a THIRD polarity again, the same one as the chat
# ceilings below: blank is the shipped default, a number is that number, and zero switches the thing off.
#
# It covers three tables nobody reads after a season - the daily assistant-token tally, the daily DVLA-lookup
# tally, and the record of what an AI assistant wrote on your behalf. Your vehicles and everything logged
# against them are NOT touched by this: they live until the account is deleted. Neither are dormant accounts,
# which are never deleted automatically at all.
#
# Thirteen months rather than twelve, so a year-on-year comparison still has a predecessor. If the privacy
# page published by this deployment states a period, it states THIS number, so changing it changes what the
# page promises.
RETENTION_LEDGER_DAYS=
# How often the sweep wakes. Blank means 24 hours.
RETENTION_INTERVAL=

# --- Auth0 Management API ---------------------------------------------------------------------------------
# A machine-to-machine application in the same tenant, authorised for the Management API with the `read:users`
# AND `delete:users` grants. The server needs it because the access token carries no email address - only
# `auth0|68a…` - and both the invitation list and the comp list are checked against the real address, and
# against whether the tenant has verified it. SO WITH THIS UNSET NOBODY REACHES THE PAID TIER, even when the
# comp list names them: an address that cannot be read is on no list. Under `SIGNUP_MODE=InviteOnly` it is
# worse than that - nobody new can be admitted at all.
# The same credential erases the Auth0 login when someone deletes their account, so with this unset
# DELETE /api/account answers 503 AND DELETES NOTHING - rather than destroying the data and leaving a working
# sign-in behind, which is the worst of both outcomes and would be silent.
AUTH0_MANAGEMENT_CLIENT_ID=
AUTH0_MANAGEMENT_CLIENT_SECRET=

# --- Adopting pre-multi-user vehicles (a one-off retrofit) --------------------------------------------------
# The single Auth0 subject (`auth0|…`, from the Auth0 dashboard) permitted to inherit vehicles that have no
# owner. Blank means no adoption, ever - which is what a fresh deployment wants, and what the retired
# "whoever signs in first claims everything" rule got wrong the moment a stranger could sign in first.
OWNERSHIP_CLAIM_UNOWNED_FOR=

# --- The in-app assistant ----------------------------------------------------------------------------------
# An Anthropic API key from platform.claude.com. Blank means the chat is OFF: /api/chat answers 503 and the app
# renders no chat entry point at all - the same polarity as `Lookup__*` above. Note this is now the DEPLOYMENT
# switch only: an account also has to be on a plan that includes the assistant (see PLANS_COMP_EMAILS above),
# and the entry point needs both.
CHAT_API_KEY=
# Blank uses the shipped default (claude-sonnet-5). Set to claude-opus-5 to trade cost for accuracy.
CHAT_MODEL=

# The daily token allowances, per account and across the whole deployment - applied to an account that HAS the
# assistant; whether it has one at all is the plan above. A THIRD POLARITY, and it is worth
# reading twice: BLANK means "use the code default" (1,000,000 and 5,000,000), while an explicit 0 TURNS THE
# CHAT OFF for that scope. So blank is generous and zero is closed - write a number to lower the ceiling.
#
# Every token the model reports counts, cached prefix included, even though a cached one is billed at a tenth.
# The tool catalogue is ~17k of that per turn, so read the per-account figure as roughly 60 turns a day rather
# than as a quantity of conversation. It is a guard rail on volume, not an invoice.
CHAT_DAILY_TOKENS_PER_OWNER=
CHAT_DAILY_TOKENS_GLOBAL=
